# WeChat Work Hook for Claude Code
# Environment Configuration Example

# ============================================================================
# Required Configuration
# ============================================================================

# WeChat Work webhook URL
# Get this URL from your WeChat Work group robot settings
CLAUDE_HOOK_WECHAT_URL="https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=YOUR_KEY_HERE"

# ============================================================================
# Optional Configuration
# ============================================================================

# Disable hook execution (used internally to prevent recursion)
# Normally you don't need to set this manually
# CLAUDE_HOOK_DISABLE=1

# Claude CLI timeout in seconds (default: 30)
# Increase this if Claude CLI takes longer to analyze session logs
# CLAUDE_HOOK_TIMEOUT=60

# Number of session log lines to extract and analyze (default: 10)
# Increase this to provide more context to Claude CLI
# CLAUDE_HOOK_LOG_LINES=20
